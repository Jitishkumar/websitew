// For MessagesScreen.js, NotificationsScreen.js, and ProfileScreen.js
const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#000033',
    paddingTop: 50, // Add safe area padding for notch/camera
  },
  // ... rest of the styles
});